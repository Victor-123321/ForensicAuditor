# Local model (Ollama) -- drives every intermediate ReAct step (FR-15).
OLLAMA_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=llama3.1:8b

# Max tool-call/reasoning steps per investigation (FR-13, NFR-3).
AGENT_MAX_STEPS=12

# Cloud model -- reserved for case-file narrative synthesis and the
# /ask follow-up ONLY, capped at <=2 calls/investigation (FR-15, NFR-4).
# Uses OpenRouter's OpenAI-compatible chat completions API so any of
# OpenRouter's free-tier models work without code changes -- pick one
# from https://openrouter.ai/models?max_price=0 and put its slug below.
CLOUD_LLM_API_KEY=
CLOUD_LLM_MODEL=nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free
# Override only if not using OpenRouter:
# CLOUD_LLM_URL=https://openrouter.ai/api/v1/chat/completions
